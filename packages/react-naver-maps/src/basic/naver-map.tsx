import React from 'react';

export function NaverMap(): React.ReactNode {
  return null;
}

// export function useNaverMap

// import React, { useRef } from 'react';
// import { MapContext } from '../contexts/map-context';
// import Marker, { useMarker } from '../overlays/marker';

// type Props = { map: naver.maps.Map };

// const NaverMap: React.FC<Props> = (props) => {
//   const { map, children } = props;

//   return (
//     <MapContext.Provider value={map}>
//       {children}
//     </MapContext.Provider>
//   );
// };
