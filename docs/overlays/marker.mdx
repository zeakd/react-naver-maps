---
name: Marker
menu: Overlays
---

import { useState, useEffect, useCallback } from 'react'
import { Playground, Props } from 'docz';
import { LoadNavermaps, NaverMap, Marker } from '../../'

# Marker

<Props of={Marker} />

# test

<Playground>
  {() => {
    return (
      <LoadNavermaps
        ncpClientId={'6tdrlcyvpt'}
        render={
          () => {
            const [point, setPoint] = useState(new naver.maps.LatLng(37.3595704, 127.105399)) 
            const [, forceUpdate] = useState()
            useEffect(() => {
              setInterval(() => {
                forceUpdate({})
              }, 1000)
            }, [])
            console.log('render')
            return <NaverMap
              center={new naver.maps.LatLng(37.3595704, 127.105399)}
            >
              <Marker 
                draggable={true}
                onPositionChanged={(position) => console.log('changed', position)}
                onDraggableChanged={(...args) => console.log('onDraggableChanged', ...args)}
              />
            </NaverMap>
          }
        }
      />
    )
  }}
</Playground>