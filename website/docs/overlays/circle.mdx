---
name: Circle
menu: Overlays
---
<!-- 
import { useState, useEffect, useCallback } from 'react'
import { Playground, Props } from 'docz';
import { LoadNavermaps, NaverMap, Circle } from '../../'

# Circle

<Playground>
  {() => {
    return (
      <LoadNavermaps
        ncpClientId={'6tdrlcyvpt'}
        render={
          () => {
            const [point, setPoint] = useState(new naver.maps.LatLng(37.3595704, 127.105399)) 
            const [strokeWeight, setStrokeWeight] = useState(10) 
            useEffect(() => {
              const id = setInterval(() => {
                setPoint(prev => {
                  return new naver.maps.LatLng(prev.lat(), prev.lng() + 0.001);
                })
              }, 1000);
              return () => { clearInterval(id) };
            }, []);
            return <NaverMap
              center={new naver.maps.LatLng(37.3595704, 127.105399)}
            >
              <Circle 
                radius={1000}
                strokeWeight={strokeWeight}
                strokeOpacity={0.5}
                strokeColor={'green'}
                strokeStyle={'dot'}
                fillColor={'red'}
                center={point}  
                clickable={true}               
                onStrokeWeightChanged={(...args) => console.log('onStrokeWeightChanged', ...args)}
              />
            </NaverMap>
          }
        }
      />
    )
  }}
</Playground> -->