---
title: 원 그리기
category: examples
order: 1
---

import Example from 'astro-component-docs/components/Example.astro';
import CircleExample from '../../../examples/circle-example.tsx';

지도 위에 원을 그리는 예제입니다.

<Example of={CircleExample} />

## 설명

`Circle` 컴포넌트의 `center`와 `radius`로 원의 위치와 크기를 지정합니다. `strokeColor`, `fillColor` 등으로 스타일을 설정할 수 있습니다.
